import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'front-end-tcc-coleta-verde',
  webDir: 'www'
};

export default config;
